# 露涼社官網 — 待辦交接

最後更新：2026-08-16

本文件記錄**已完成的優化**、**需要圖片支援的項目（交接 Codex）**、以及**需要老闆決策才能動的項目**。
程式面可自行判斷的問題已全部修完，詳見 git log。

---

## 一、需要圖片（交接 Codex／需本機影像工具）

本 session 的執行環境沒有影像轉檔工具（無 cwebp／PIL），且不應把他站商品照放上商業網站，
因此以下項目只完成版面與欄位預留，圖片本身需另行產出。

### 1. 縮小 header logo（優先）

| 項目 | 內容 |
|---|---|
| 產出檔 | `assets/logo-88.webp`（或 .png），**88×88**（44px @2x），目標 3-6KB |
| 現況問題 | 8 個頁面的 header 都用 `assets/logo.jpg`（**1024×1024 / 68KB**）硬塞進 44px 框 |
| 要改的地方 | 各頁 header `<img>` 的 `src`；另 `btu-guide.html` 的 logo `width/height` 由 1024 改 36 |
| **不要改** | `assets/logo.jpg` 檔案本身要保留，且 7 處 JSON-LD 的 `logo` 欄位仍指向它（Google 建議 logo 用較大尺寸） |

### 2. SAC688 產品圖轉 webp

| 項目 | 內容 |
|---|---|
| 產出檔 | `assets/sac688_product.webp`，**640×640** |
| 現況問題 | `sac688_product.jpg` **82KB**，同一格並排的 `juz400_product.webp` 只要 **12KB** |
| 要改的地方 | `index.html` 對比卡的 `<img src>` |
| **不要改** | `assets/sac688_product.jpg` 要保留，Product JSON-LD 的 `image` 仍指向它 |

### 3. og-cover 頁內插圖轉 webp

| 項目 | 內容 |
|---|---|
| 產出檔 | `assets/og-cover.webp`，**1200×630** |
| 現況問題 | `og-cover.jpg` **253KB**，被當成頁內 `<img>` 直接顯示 |
| 要改的地方 | 只改 `index.html` 頁內 `<img>` 的 src（或用 `<picture>` 包 webp + jpg fallback），保持 `width="1200" height="630" loading="lazy"` |
| **不要改** | `og:image` / `twitter:image` / JSON-LD `image` 一律維持指向 `.jpg`（社群平台對 webp 支援不一） |

### 4. 「其它小物」14 品項實拍照（新需求）

新分頁 `其它小物` 的每張卡片左側已預留 `.ad-photo` 欄位（56×56 圓角框），
目前放 emoji 示意圖示。**放實拍只需把 `<img>` 塞進 `.ad-photo`，不必改版面**：

```html
<div class="ad-photo"><img src="assets/addon-juz-tarp.webp" alt="5×8 黑膠天幕"></div>
```

需要的照片（建議 **正方形 320×320 webp，單張 < 20KB**，淺色或白底，拍實際庫存品）：

| # | 品項 | 建議檔名 |
|---|---|---|
| 1 | 冰虎 ALPICOOL C40 移動冰箱 40L | `addon-fridge-c40.webp` |
| 2 | 5×8 黑膠天幕 | `addon-tarp-58.webp` |
| 3 | 黑狗速開穹頂 | `addon-blackdog-dome.webp` |
| 4 | BLUETTI 電吉拉 mini 行動電站 | `addon-bluetti-mini.webp` |
| 5 | 動力延長線（綠）5M | `addon-cable-5m.webp` |
| 6 | 動力延長線（綠）10M | `addon-cable-10m.webp` |
| 7 | 五米黃光燈條 | `addon-lightstrip-5m.webp` |
| 8 | 十米黃光燈條 | `addon-lightstrip-10m.webp` |
| 9 | 持久帳篷小燈 | `addon-tent-lamp.webp` |
| 10 | 簡易焚火台 | `addon-firepit-basic.webp` |
| 11 | 青鸞觀火台 | `addon-firepit-qingluan.webp` |
| 12 | 投影機 | `addon-projector.webp` |
| 13 | 製冰機 | `addon-icemaker.webp` |
| 14 | ADAM OUTDOOR 渦輪扇 | `addon-adam-fan.webp` |

> ⚠️ **請用自家實際庫存品拍攝**，不要用廠商或電商的商品圖（著作權風險，且實拍對客戶更有說服力）。

### 5. （選用）首頁大圖

`assets/homepage_hero.png` **1531KB** 與 `taiwan_map.png` **908KB** 目前**全站沒有任何頁面使用**。
sitemap 原本錯誤宣告它們是首頁圖片，已移除。若之後要放上首頁，需先轉 webp 並確認尺寸，
再照現有格式帶 `width/height/loading="lazy"` 與中文 alt。

---

## 二、需要老闆決策（AI 不應自行認定）

### 1. 🔴 機型適用坪數：站上有四套互斥規則（最優先）

同一頂 **300×300cm（9㎡ ≈ 2.7 坪）** 的標準四人帳，目前在網站上會得到**相反的推薦**：

| 位置 | 規則 | 2.7 坪帳的結果 |
|---|---|---|
| 首頁「坪數計算機」 | `area >= 4.0 坪 → SAC688` | 推薦 **JUZ-400** |
| 首頁「帳篷計算機」 | `area > 7 ㎡（=2.1 坪）→ SAC688` | 推薦 **SAC688** ⚠️ |
| 首頁規格卡／JSON-LD | JUZ「3-5 坪」、SAC「5-8 坪」 | 標示 JUZ |
| btu-guide 比較表 | JUZ「3×3 以下」、SAC「3×3～4×4」 | 邊界值 |

而且首頁同一張卡片內就自相矛盾：標題寫「3×3 以下」（=2.7 坪），內文卻寫「適合 3-5 坪」。

**需要你拍板**：JUZ-400 與 SAC688 的分界到底是多少？（建議以帳篷邊長表述，例如
「JUZ：3×3 以下」「SAC：3×3～4×4」），我再一次改齊 8 處文案 + 2 支計算機門檻。

### 2. 🟠 `aggregateRating` 4.95 / 160 則的來源

首頁與 reviews.html 宣告平均 4.95 分、160 則評價，但頁面只有 5 張截圖。
**若沒有可佐證的實際評論數（Google 評論？LINE 對話？），建議調整為真實數字** ——
灌水的評分結構化資料可能被 Google 拿掉複合式搜尋結果，甚至觸發人工處分。

### 3. 🟠 LocalBusiness 缺少 address（Google 必填）

首頁 LocalBusiness 沒有 `address` 欄位。需要你決定對外顯示的**主要據點縣市**
（只要縣市層級，不需門牌）。站內既有寫法：`新竹市`／`台北市`／`台中市西屯區`。
首頁 geo 座標（24.8138, 121.0177）落在竹北一帶，若要與座標一致可用「竹北市」。
**我不會自行編造地址。**

### 4. 🟡 營區資料庫實際筆數

首頁原本一處寫「200+ 筆」、其他 9 處寫「600+ 筆」。已統一為 **600+**（多數一致）。
本環境網路政策擋掉 workers.dev，我無法實查 API。**請你確認實際筆數**，若不是 600+ 我再改。

### 5. 🟡 三項小物的型號規格

以下品項頁面上已標示「型號規格確認中」，請提供正確型號後我補上：
- **投影機** — 品牌型號、流明、電源需求
- **製冰機** — 品牌型號、製冰量、耗電（**耗電很重要**，會影響營區用電建議）
- **ADAM OUTDOOR 渦輪扇** — 確切型號（ADAM 有多款：隨身冰鎮風扇／無線 DC 循環扇）

另外，以下規格取自公開資料，**請你對照實機確認**：
- 冰虎 ALPICOOL C40：40L／約 14kg／約 60W／-20°C~+20°C
- BLUETTI 電吉拉 mini：1024Wh／1800W 輸出／約 11.3kg
- 5×8 黑膠天幕、黑狗速開穹頂：目前只寫通用描述，未標尺寸重量

### 6. 🟡 小物的租借條件

頁面目前寫「建議與冷氣一起預約」，但沒說死。請確認：
- 小物**可否單獨租借**（不租冷氣）？
- 小物**要不要收押金**？（目前只有冷氣寫明押金 $3,000／台）
- 小物跨天計價方式？（目前全部標「／次」，比照冰箱）

### 7. 🟢 雨天改期政策

三個頁面對雨天／颱風的說法寬鬆度不一。已把首頁對齊 `pricing.md` 最保守的說法
（「颱風、豪大雨、交通中斷等不可抗力：可提供公告或截圖協商」），
並移除 FAQ 入口未兌現的「雨天改期」字樣。

**若你希望對外提供「雨天可改期」，需要你確認條件**，我再新增 FAQ 題目並統一三頁說法。

---

## 三、本輪已完成（摘要）

- **事實一致性**：用電加總 5.3A→4.8A（算錯，汙染兩頁 FAQ 結構化資料）、BTU 6000→6300
  （分享預覽標題傳錯規格）、冰箱單位／台→／次、營區筆數統一
- **揭露缺口**：首頁補上押金 $3,000、租金包含範圍、需自備／可加購延長線
- **廣告追蹤**：13 頁補齊 gtag（原本 16 頁零追蹤）、9 個死碼 onclick 修復、
  21 個 LINE 出口補上轉換、修正 emergency-ac 從未回傳轉換（該頁正在投廣告）
- **無障礙**：查詢欄位可及名稱、取機點 aria-pressed、輪播暫停鈕、
  tablist 語意、色彩對比、reduced-motion 覆蓋完整化
- **行動裝置**：子頁籤吸頂被 header 遮擋、瀏海機安全區、頁尾觸控目標 17px→39px
- **表單**：取機日期不能選過去、移除語意重複的「取機方式」欄位
- **SEO**：移除無可見內容的首頁 FAQPage（政策風險）、Product 實體去重、
  28 處 div→h2、8 頁 meta description 壓到 80 字內、孤兒頁補內鏈、sitemap 清理假圖
- **版面穩定**：9 處圖片宣告尺寸對齊實際檔案（原本兩張錯誤造成 CLS）
- **新功能**：首頁公告輪播新增父親節抽獎、冰箱分頁改版為「其它小物」（14 品項）

驗證方式：`node scripts/validate-site.mjs` + Playwright 於 390×844 實機尺寸操作驗收。
