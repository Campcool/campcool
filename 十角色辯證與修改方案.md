# campcool 前台：十角色腦力激盪與滿分制修改方案

**日期**：2026-08-16　**起點**：Claude 完成 20 commits 後約 94–95 分　**目標**：超過 100 分線（滿分制 100 分制）

## 一、十角色腦力激盪

**主管**：分數 94–95，剩下的分差在「可掌控未掌控」。AI-README 寫得很好，但沒人（機器）自動守門——validate 存在卻沒有 CI 跑它。滿分制最重的兩維（穩定 25／可維護 20）都指向同一件事：**把既有防回歸規則接上自動化門禁**。方向定了：不新增業務功能，把守門制度補完。

**架構師**：現況是「純靜態零建置」單體，18 頁 HTML 各自為政。Claude 的選擇是內嵌風格（69KB inline CSS），這不是壞事——它消除了建置管線失敗面。我同意維持：抽離 CSS/JS 在零建置 repo 裡會引入新失敗面（快取不同步、相對路徑錯），收益（快取複用）在「社群分享輔助工具」定位下微不足道。**唯一架構債務是 areas 三殘頁**：meta-refresh 但無 noindex，搜尋引擎與 AI 爬蟲仍會索引它們再被轉走，產生垃圾訊號。補 noindex 是最小干預解。

**資安**：GA4 ID 不進 repo、從 Worker /public/config 動態拉取——這比硬編碼更好（ID 輪替不用改站）。但兩個風險要蓋：① Worker 掛掉時 `ga4MeasurementId` 空字串，網站靜靜無聲地失去 GA4 數據，沒有人知道（**可掌控未掌控，扣分點**）；② `fetch` 無 timeout，慢回應可能拖慢首屏。防禦：加 `AbortController` 3 秒逾時 + validate 斷言「GA4 config endpoint 存在且回有效 JSON」。

**效能**：repo 裡躺著 1.5MB homepage_hero.png 與 912KB taiwan_map.png，**完全未被引用**——dead weight 先砍。reviews 的 5 張 jpg 未轉 webp，是 Claude webp 化工程的漏網之魚，補上。favicon 三件套（ico/32/192/512）與 apple-touch-icon 齊全，PWA manifest 不需要（非 Web App 定位）。首頁 188KB 的 inline CSS/JS 是架構選擇，不碰。

**SEO／GEO**：本站定位是行銷輔助工具而非 SEO，所以不追求關鍵字密度。但 GEO（AI 爬蟲）是既定的（robots.txt 已開 GPTBot 等）：**llms.txt 是 GEO 的主戰場**。現況：日期停在 07-27，而 08-16 改版改了機型分界、押金、15 品項小物——llms.txt 與正式頁內容已有落差。更新 llms.txt 是 GEO 層面補到 100 的必要動作。areas 殘頁 noindex 防止索引垃圾也在此列。

**產品**：15 品項小物是 Claude 這輪的產品大禮（分類抽屜＋勾選帶入表單）。待老闆確認的 7 項與缺的 7 張商品照是**業主內容天花板**——AI 不該代造，在 AI-README 與本方案明確標記，這在滿分制中「列明外部依賴」即是得分。試算閉環已在兩支計算機統一（3×3 分界），validate 需補「兩支計算機門檻必須同值」的跨函式斷言——這是本倉獨有的防矛盾機制，比一般斷言更值錢。

**UX**：每頁 CTA 充足（1–5 個），LINE 預填流程與告知文案齊備，reduced-motion 與 aria-live 規則已由 Claude 寫進防回歸。剩一個盲點：**手機 390px 水平溢出與 LINE CTA 觸控高度 44px 目前是「手動檢查項」，沒人自動守**。CI 可加 `html-validate` 級別檢查有限，但 validate-site.mjs 可以機械檢查「所有 lin.ee 連結的類別含線索最小尺寸樣式」——實際做法：CSS 層 `.line-cta` 已有 min-height？驗證後再定。為控制範圍，本輪以「CSS 含 `.line-cta` 的 min-height/44px 規則斷言」處理。

**使用者**（模擬）：我是露營新手，進站看價格 → 選機型 → 預約 → LINE 送出。全程最怕三件事：價格不誠實（押金藏在 FAQ）、機型算錯（上次 300×300 帳得到兩個答案）、LINE 送出前我的資料被偷傳（已有明確告知文案）。這三項目前都已被 validate 守住。**剩下的擔心是：沒人保證它永遠被守住**——所以門禁是使用者視角的滿分題。

**維運**：沒有 CI＝沒有安全網，一次手滑推 main 就直接上線。滿分制的維運答案：site-check.yml 三 job——①validate-site.mjs ②inline JS 語法與 JSON-LD 檢查（validate 內已有）③**防假綠自檢**（故意改壞一個常數、確認 validate 抓住、再還原）。參考 0988145875 的成功模式。GitHub Pages 無 build 步驟，所以 CI 就是「跑驗證＝門禁」。

**QA**：防假綠的覆蓋面要問：哪些斷言有自檢？全部都要有，但實作成本不同。策略：validate-site.mjs 自帶 `--selftest` 模式（同一個腳本、同一份斷言集跑正反兩遍），CI 先跑 `--selftest` 確認腳本本身可靠，再跑正常檢查。這是五倉裡最穩的防假綠形式（腳本與自檢同源，不可能腳本改版而自檢過期）。

## 二、辯證決議（反對意見與裁決）

| 爭議 | 反對意見 | 裁決 |
|---|---|---|
| 抽離 index 的 inline CSS/JS 降 188KB | 打破零建置、引入快取不同步新失敗面 | **不抽離**。架構師＋維運一致：風險>收益 |
| 補 PWA manifest | 站不是 Web App，無離線需求 | **不做** |
| 機型頁（juz-400/sac-688）補獨立 JSON-LD 與試算？ | 已是 Claude 改版範圍；本輪只補防回歸 | **只做防回歸** |
| areas 殘頁直接刪除 | meta-refresh 仍服務外部舊連結（社群貼文、LINE 群組分享的歷史網址） | **保留＋補 noindex**，連結不中斷 |
| GA4 config endpoint 掛掉的監控 | 正式監控平台超出靜態站範圍 | **CI 測一次 endpoint 可用性**＋validate 斷言；線上監控列業主待辦 |
| reviews 5 張 jpg 轉 webp | og/image 政策要求 jpg，AI 可能誤傷 | **只轉 page-level img 引用**（review-01~05），meta/JSON-LD 維持 jpg |
| 15 品項小物照片缺 7 張 | 業主待辦天花板，AI 代造即造假 | **不做**，AI-README 列明 |

## 三、修改清單（按優先序）

**P0（制度，滿分制核心）**
1. `scripts/validate-site.mjs` 擴充：GA4 config endpoint 可用性、兩支計算機門檻一致性（3×3 分界）、每頁 logLineClick/gtag 覆蓋、LINE CTA 44px 規則、llms.txt 價格與頁面對照、殘頁 noindex、死重圖片缺席（homepage_hero/taiwan_map 不在 repo）、review webp 化斷言——目標 20+ 項斷言
2. 同一腳本加 `--selftest` 防假綠模式（正反斷言雙跑）
3. `.github/workflows/site-check.yml`：push/PR 跑 validate＋selftest＋線上 endpoint 探針；GitHub Pages 無 build，驗證即門禁

**P1（效能與成本）**
4. git rm 死重圖片：homepage_hero.png（1.5MB）、taiwan_map.png（912KB）
5. reviews 5 張 jpg→webp（img 層），頁面引用更新，JSON-LD/og 維持 jpg

**P2（GEO 與可維護）**
6. 更新 llms.txt 至 08-16 改版後內容（機型分界、押金、小物 15 品項、用電 4.8A/5.7A）
7. areas 三殘頁補 `<meta name="robots" content="noindex">`
8. index GA4 fetch 加 3 秒 AbortController 逾時（風險極小改動）

**P3（交接）**
9. AI-README.md 插入 Manus 滿分制條目（含本次辯證決議、新斷言清單、selftest 用法）

**風險管制**：每次改動後本地跑 validate；CI 全綠才推送；AI-README 與程式碼同 commit。不動任何 Claude 已定的產品事實（機型分界、押金、LINE 流程、取件政策）。
